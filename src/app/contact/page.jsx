import ContactContent from "./ContactContent";

export const metadata = {
  title: "Contact — NexEvent",
  description:
    "Get in touch with the NexEvent team. We’re here to help you launch and scale unforgettable events.",
};

export default function ContactPage() {
  // Render a client component where AOS is initialized
  return <ContactContent />;
}